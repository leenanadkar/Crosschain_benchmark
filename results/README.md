# Benchmark Results

This directory stores generated benchmark outputs.

Run:

```bash
npm run benchmark:gas
npm run benchmark:summary
npm run plot:gas
```

The checked-in `gas-results.json` contains the raw deployment and execution gas values
generated by `test/gasBenchmark.test.js` on a local Ganache EVM instance.
